{
  "name": "semantic-release",
  "version": "16.2.0",
  "private": true,
  "scripts": {
    "release": "source .env.local && GITHUB_TOKEN=$GITHUB_TOKEN npx semantic-release"
  },
  "devDependencies": {
    "semantic-release": "^21.0.7"
  },
  "engines": {
    "node": ">=18"
  }
}
